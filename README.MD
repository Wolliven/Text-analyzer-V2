This program takes in a folder and analyzes all the .txt files, giving basic statistics about them.
The program then outputs either a summary to the terminal or a JSON file.
***
USAGE (on terminal): python main.py [folder_name] [Output JSON file name (optional)]
***